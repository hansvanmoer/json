#ifndef JSON_BUFFERED_READER_H
#define JSON_BUFFERED_READER_H
/*
#include "JSON.h"

namespace JSON{

	template<typename Character_, typename CharTraits_, typename Allocator_> class BasicDataPolicy{
		public:
			using Character = Character;
			using CharTraits = CharTraits_;
			using Allocator = Allocator_;
			
			using InputStream = std::basic_istream<Character, CharTraits>;
			
			using String = std::basic_string<Character, CharTraits, Allocator>;
			using Number = double;
			using Boolean = bool;
		protected:
	};

	template<typename DataPolicy> class ASCIIEncodingPolicy{
		public:
			void prepareInputStream(typename DataPolicy::InputStream &input){};
	};

	template<typename DataPolicy_, template<typename> class EncodingPolicy_> class BufferedDocumentReader : private DataPolicy, private EncodingPolicy_<DataPolicy_>{
		
		class Document{
		
		};

		class DocumentBuilder{
		
		};

		
	};



}
*/
#endif
